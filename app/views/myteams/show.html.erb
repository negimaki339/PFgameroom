<div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class='col-md-3'>
      <h2>User info</h2>
      <%= render 'users/info', user: current_user %>


    </div>
        マイチーム詳細画面
        <td>ゲーム名<%= @team.game_name %></td>
        <td>チーム名<%= @team.team_name %></td>
        <td>チーム概要<%= @team.overview %></td>
        <td><%= link_to 'Edit', edit_myteam_path(@team) %></td>
  </div>
</div>