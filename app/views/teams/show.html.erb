<div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class='col-md-3'>
        <h2>User info</h2>
        <%= render 'users/info', user: current_user %>
    </div>
        <div class='col-md-9'>
            <table class="table table-bordered">
              <h1 class="my-3 ml-3">チーム詳細画面</h1>
              <thead>
                 <tr>
                    <th>ゲーム名</th>
                    <th>チーム名</th>
                    <th>リーダー名</th>
                 </tr>
              </thead>
                 <tbody>
                   <tr>
                    <td><%= @team.game_name %></td>
                    <td><%= @team.team_name %></td>
                    <td><%= @team.user.name %></td>
                   </tr>
                  </tbody>
              </table>
            <table class="table table-bordered">
              <thead>
                <tr>
                 <th>チーム説明</th>
                </tr>
              </thead>
              <tbody>
                <tr>
                  <td><%= @team.team_explanation %></td>
                </tr>
              </tbody>
            </table>

            <%= form_with(model: [@team, @member],url: myteam_members_path(@team) ) do |f| %>
            <%= hidden_field_tag :team_id, @team.id %>
            <%= f.submit "メンバーになる" %>
            <% end %>
        </div>
  </div>
</div>
